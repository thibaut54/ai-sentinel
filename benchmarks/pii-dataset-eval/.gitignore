# Generated, dataset-derived or cache artifacts — regenerated by build_datasets.py.
# Kept out of VCS (licensing of HF dataset text + reproducible from frozen revisions).
gold/
mappings/
__pycache__/
*.pyc
# Real endpoint config (may carry private LAN URLs); commit extractors.sample.json instead.
extractors.json
